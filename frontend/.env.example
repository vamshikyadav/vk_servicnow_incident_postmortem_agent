# Copy to .env.local for local dev
# cp .env.example .env.local

# URL of the backend API (no trailing slash).
# Leave empty to use Vite's built-in dev proxy (localhost:8080).
# Set this to the Cloud Run / GKE backend URL for production builds.
VITE_API_URL=
